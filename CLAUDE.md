@AGENTS.md

## Claude Code

- Keep this file short. Project-wide rules live in `AGENTS.md`; detailed workflow lives in `skills/chatlog-http-cli/SKILL.md`.
- Use `/memory` to confirm this file is loaded if Claude appears to ignore project instructions.
- Prefer updating root state files over relying on conversation history when work spans sessions.

